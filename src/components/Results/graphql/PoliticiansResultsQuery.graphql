query PoliticiansResults($quizSlug: String!) {
    politicianResultsConnection(sort: "featured:desc", where: { quiz_slug: $quizSlug } ) {
        values {
            rid
            politician {
                name
                image {
                    url
                }
            }
        }
    }
}
